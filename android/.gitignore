*.iml
.gradle/
/local.properties
.idea/
.DS_Store
build/
app/build/
captures/
.externalNativeBuild/
.cxx/
# Bundled ArcFace models (per-flavor, large binaries) — not in git (see copy-model.ps1)
*.onnx
# Release signing secrets — NEVER commit
*.keystore
keystore.properties
build_log.txt
build_release_log.txt
_*.log
# built APKs (large; distribute out-of-band)
*.apk
