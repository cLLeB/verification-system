# Verification system dependencies
# Install:  pip install -r requirements.txt

# --- Face recognition (current focus) ---
insightface>=0.7        # ArcFace embeddings + face detection (buffalo_l model pack)
onnxruntime>=1.16       # CPU inference for the InsightFace/CCNet ONNX models (no GPU)

# --- Palm recognition (second modality) ---
mediapipe>=0.10.18      # hand-landmark detection (Tasks API) for palm ROI extraction
                        # (palm recognition itself uses the built-in Gabor encoder;
                        #  a trained CCNet->ONNX is an optional accuracy upgrade)

# --- Shared ---
opencv-contrib-python>=4.8
numpy>=1.24
flask>=3.0
flask-cors>=4.0
cryptography>=42.0       # template encryption at rest (set FACE_DB_KEY to enable)
pyopenssl>=23.0          # self-signed HTTPS (needed for the phone camera)

# --- Archived fingerprint system (in ./fingerprint) ---
scipy>=1.10
scikit-image>=0.22
fingerprint-enhancer>=0.0.14
fingerprint-feature-extractor>=0.0.10
jpype1>=1.5              # bridge to SourceAFIS (needs Java 21 + jars in fingerprint/libs)

# --- Dev/test ---
pytest>=8.0
